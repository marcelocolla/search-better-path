import React from 'react'
import Typography from '@material-ui/core/Typography'

const OutputGenerated = () => {
  // Should display the empty state when not found results
  // Should display the loading when the script is being processed
  // Should display the results when the script is complete
  return (
    <main>
      <Typography>output results</Typography>
    </main>
  )
}

export default OutputGenerated

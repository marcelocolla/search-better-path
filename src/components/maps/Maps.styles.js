import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  root: {
    backgroundColor: '#fff',
    borderRadius: 16,
    overflow: 'hidden',
    display: 'flex',
  },
})

export default useStyles
